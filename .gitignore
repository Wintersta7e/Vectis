# Build
build/
build-*/
build-win-*.log
cmake-build-*/
out/

# Windows build driver (local convenience, not part of the source tree)
build-windows.cmd

# IDE / local tool state
.vs/
.vscode/
.idea/
.claude/
.serena/
.cache/
*.swp
*.swo
*~

# vcpkg
vcpkg_installed/

# Binary output
/vectis
/vectis.exe
vectis-data/

# Release artifacts (built by scripts/build-agent-kit.sh)
/vectis-agent-kit-*.tar.gz

# Generated digests (Vectis produces these next to scanned projects)
vectis-digest.json
vectis-digest.md
vectis-digest-slim.json

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Secrets
.env
*.pem
*.key
vectis.toml

# CLAUDE.md is local project instructions only
CLAUDE.md

# Local reference notes (not part of the source tree)
external-project-notes.txt
tests/fixtures/code/sample-multimodule/

# Internal planning docs (not for the repo)
docs/design/
docs/BUILD.md
docs/plans/
docs/superpowers/

# Local validation harness (real-world OSS repos cloned for smoke runs)
testing-ground/
